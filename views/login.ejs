<!DOCTYPE html>
<html lang="es">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login - Frutotos</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;500;600;700&display=swap" rel="stylesheet">
    <script>
        tailwind.config = {
            theme: {
                fontFamily: {
                    sans: ['Poppins', 'sans-serif'],
                }
            }
        }
    </script>
    <style>
        body {
            font-family: 'Poppins', sans-serif;
        }
    </style>
</head>

<body class="bg-gradient-to-br from-teal-500 to-green-700 h-screen flex items-center justify-center p-4">
    <div
        class="bg-white/90 backdrop-blur-md p-10 rounded-2xl shadow-2xl w-full max-w-sm border border-white/20 relative overflow-hidden">
        <!-- Decorative blob -->
        <div
            class="absolute -top-10 -right-10 w-32 h-32 bg-green-200 rounded-full mix-blend-multiply filter blur-xl opacity-70 animate-pulse">
        </div>
        <div
            class="absolute -bottom-10 -left-10 w-32 h-32 bg-teal-200 rounded-full mix-blend-multiply filter blur-xl opacity-70 animate-pulse animation-delay-2000">
        </div>

        <div class="text-center mb-8 relative z-10">
            <img src="/images/logo.png" alt="Frutotos Logo"
                class="h-12 mx-auto mb-4 hover:scale-110 transition-transform duration-300">
            <h1 class="text-2xl font-bold text-gray-800 tracking-tight">Acceso Administrativo</h1>
            <p class="text-green-600 text-sm font-medium">Gestión de Embajadores</p>
        </div>

        <% if (locals.error) { %>
            <div class="bg-red-50 border-l-4 border-red-500 text-red-700 p-4 rounded mb-6 text-sm shadow-sm relative z-10"
                role="alert">
                <span class="block sm:inline font-medium">
                    <%= error %>
                </span>
            </div>
            <% } %>

                <form action="/login" method="POST" class="relative z-10 space-y-6">
                    <div>
                        <label class="block text-gray-700 text-xs font-bold mb-2 uppercase tracking-wide"
                            for="username">
                            Usuario
                        </label>
                        <input
                            class="block w-full px-4 py-3 text-gray-700 bg-gray-50 border border-gray-200 rounded-lg focus:outline-none focus:bg-white focus:border-green-500 focus:ring-2 focus:ring-green-200 transition duration-200"
                            id="username" name="username" type="text" placeholder="Ingresa tu usuario" required>
                    </div>
                    <div>
                        <label class="block text-gray-700 text-xs font-bold mb-2 uppercase tracking-wide"
                            for="password">
                            Contraseña
                        </label>
                        <input
                            class="block w-full px-4 py-3 text-gray-700 bg-gray-50 border border-gray-200 rounded-lg focus:outline-none focus:bg-white focus:border-green-500 focus:ring-2 focus:ring-green-200 transition duration-200"
                            id="password" name="password" type="password" placeholder="••••••••" required>
                    </div>
                    <div>
                        <button
                            class="w-full bg-gradient-to-r from-green-600 to-teal-600 hover:from-green-700 hover:to-teal-700 text-white font-bold py-3 px-4 rounded-lg focus:outline-none focus:shadow-outline transform transition hover:-translate-y-0.5 shadow-lg"
                            type="submit">
                            Iniciar Sesión
                        </button>
                    </div>
                </form>
                <div class="text-center mt-8 relative z-10">
                    <p class="text-gray-400 text-xs">&copy; 2026 Frutotos.</p>
                    <p class="text-gray-500 text-[10px] mt-1">
                        Creado por <a href="https://www.linkedin.com/in/nicocabrera-marketing/" target="_blank"
                            class="text-green-600 hover:text-green-700 hover:underline transition font-bold">Nicolas
                            Cabrera</a>
                    </p>
                </div>
    </div>
</body>

</html>