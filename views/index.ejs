<!DOCTYPE html>
<html lang="es">

<head>
    <meta charset="UTF-8">
    <title>Panel de Atletas | Frutotos</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;500;600;700&display=swap" rel="stylesheet">
    <script>
        tailwind.config = {
            theme: {
                fontFamily: {
                    sans: ['Poppins', 'sans-serif'],
                },
                extend: {
                    colors: {
                        frutotos: {
                            green: '#4CAF50', // Ajustar al código exacto si se tuviera
                            dark: '#2E7D32',
                            light: '#E8F5E9'
                        }
                    }
                }
            }
        }
    </script>
</head>

<body class="bg-gray-50 font-sans min-h-screen">
    <!-- Navbar -->
    <nav class="bg-white shadow-sm border-b border-gray-100 sticky top-0 z-50 backdrop-blur-lg bg-white/90">
        <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
            <div class="flex justify-between h-20">
                <div class="flex items-center gap-3">
                    <img src="/images/logo.png" alt="Frutotos" class="h-10 w-auto hover:opacity-80 transition">
                    <span class="text-xl font-bold text-gray-800 tracking-tight hidden sm:block">Panel
                        Embajadores</span>
                </div>
                <div class="flex items-center gap-4">
                    <a href="/sync"
                        class="group flex items-center gap-2 px-4 py-2 bg-indigo-50 text-indigo-700 rounded-full hover:bg-indigo-100 transition duration-200 font-medium">
                        <span class="group-hover:rotate-180 transition-transform duration-500">🔄</span>
                        <span class="hidden sm:inline">Sincronizar</span>
                    </a>
                    <a href="/logout"
                        class="text-gray-500 hover:text-red-600 font-medium transition duration-200 text-sm">
                        Cerrar Sesión
                    </a>
                </div>
            </div>
        </div>
    </nav>

    <div class="max-w-6xl mx-auto px-4 sm:px-6 lg:px-8 py-10">

        <!-- Create Card -->
        <div class="bg-white rounded-2xl shadow-xl overflow-hidden mb-10 border border-gray-100">
            <div class="bg-gradient-to-r from-green-600 to-teal-600 px-8 py-6">
                <h2 class="text-white text-xl font-bold flex items-center gap-2">
                    <svg xmlns="http://www.w3.org/2000/svg" class="h-6 w-6" fill="none" viewBox="0 0 24 24"
                        stroke="currentColor">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                            d="M12 6v6m0 0v6m0-6h6m-6 0H6" />
                    </svg>
                    Registrar Nuevo Atleta
                </h2>
                <p class="text-green-100 text-sm mt-1 opacity-90">Crea un cupón de descuento automáticamente en Shopify.
                </p>
            </div>

            <form action="/create-affiliate" method="POST" class="p-8">
                <div class="grid grid-cols-1 md:grid-cols-3 gap-6 mb-8">
                    <div class="col-span-1">
                        <label class="block text-gray-700 text-sm font-bold mb-2">Nombre Completo</label>
                        <input type="text" name="name" placeholder="Ej: Juan Pérez"
                            class="w-full px-4 py-3 rounded-lg border border-gray-200 focus:border-green-500 focus:ring-2 focus:ring-green-100 focus:outline-none transition"
                            required>
                    </div>
                    <div class="col-span-1">
                        <label class="block text-gray-700 text-sm font-bold mb-2">Correo Electrónico</label>
                        <input type="email" name="email" placeholder="juan@ejemplo.com"
                            class="w-full px-4 py-3 rounded-lg border border-gray-200 focus:border-green-500 focus:ring-2 focus:ring-green-100 focus:outline-none transition"
                            required>
                    </div>
                    <div class="col-span-1">
                        <label class="block text-gray-700 text-sm font-bold mb-2">Código Descuento</label>
                        <div class="relative">
                            <span class="absolute left-4 top-3 text-gray-400 font-bold">#</span>
                            <input type="text" name="code" placeholder="JUAN10"
                                class="w-full pl-8 pr-4 py-3 rounded-lg border border-gray-200 focus:border-green-500 focus:ring-2 focus:ring-green-100 focus:outline-none transition font-bold uppercase tracking-wide text-green-700"
                                required>
                        </div>
                    </div>
                </div>

                <div class="grid grid-cols-1 md:grid-cols-2 gap-6 mb-8">
                    <div>
                        <label class="block text-gray-700 text-sm font-bold mb-2">% Descuento Cliente</label>
                        <div class="relative">
                            <input type="number" name="discount" placeholder="10"
                                class="w-full px-4 py-3 rounded-lg border border-gray-200 focus:border-green-500 focus:ring-2 focus:ring-green-100 focus:outline-none transition"
                                required>
                            <span class="absolute right-4 top-3 text-gray-400 font-bold">%</span>
                        </div>
                    </div>
                    <div>
                        <label class="block text-gray-700 text-sm font-bold mb-2">% Comisión Atleta</label>
                        <div class="relative">
                            <input type="number" name="commission" placeholder="10"
                                class="w-full px-4 py-3 rounded-lg border border-gray-200 focus:border-green-500 focus:ring-2 focus:ring-green-100 focus:outline-none transition"
                                required>
                            <span class="absolute right-4 top-3 text-gray-400 font-bold">%</span>
                        </div>
                    </div>
                </div>

                <div class="flex justify-end">
                    <button type="submit"
                        class="bg-gray-900 text-white px-8 py-3 rounded-lg hover:bg-black font-bold shadow-lg transform transition hover:-translate-y-1">
                        Generar Cupón & Registrar
                    </button>
                </div>
            </form>
        </div>

        <!-- List Card -->
        <h3 class="text-2xl font-bold text-gray-800 mb-6 flex items-center gap-2">
            <span class="bg-green-100 text-green-700 p-2 rounded-lg text-sm">👥</span>
            Listado de Atletas
        </h3>

        <div class="bg-white rounded-2xl shadow-lg border border-gray-100 overflow-hidden">
            <div class="overflow-x-auto">
                <table class="w-full text-left border-collapse">
                    <thead>
                        <tr class="bg-gray-50 border-b border-gray-200">
                            <th class="py-4 px-6 text-xs font-bold text-gray-500 uppercase tracking-wider">Atleta</th>
                            <th class="py-4 px-6 text-xs font-bold text-gray-500 uppercase tracking-wider">Código</th>
                            <th class="py-4 px-6 text-center text-xs font-bold text-gray-500 uppercase tracking-wider">
                                Descuento</th>
                            <th class="py-4 px-6 text-center text-xs font-bold text-gray-500 uppercase tracking-wider">
                                Comisión</th>
                            <th class="py-4 px-6 text-center text-xs font-bold text-gray-500 uppercase tracking-wider">
                                Estado</th>
                            <th class="py-4 px-6 text-center text-xs font-bold text-gray-500 uppercase tracking-wider">
                                Acciones</th>
                        </tr>
                    </thead>
                    <tbody class="divide-y divide-gray-100">
                        <% affiliates.forEach(function(atleta) { %>
                            <tr class="hover:bg-green-50/50 transition duration-150">
                                <td class="py-4 px-6 font-medium text-gray-900">
                                    <%= atleta.name %>
                                        <div class="text-xs text-gray-400 font-normal">
                                            <%= atleta.email %>
                                        </div>
                                </td>
                                <td class="py-4 px-6">
                                    <span
                                        class="bg-gray-100 text-gray-700 px-3 py-1 rounded-md font-mono font-bold text-sm border border-gray-200">
                                        <%= atleta.shopify_code %>
                                    </span>
                                </td>
                                <td class="py-4 px-6 text-center text-gray-600">
                                    <%= atleta.discount_percent %>%
                                </td>
                                <td class="py-4 px-6 text-center font-bold text-green-600">
                                    <%= atleta.commission_percent %>%
                                </td>
                                <td class="py-4 px-6 text-center">
                                    <span
                                        class="px-3 py-1 rounded-full text-xs font-semibold uppercase tracking-wide <%= atleta.status == 'active' ? 'bg-green-100 text-green-800' : 'bg-red-100 text-red-800' %>">
                                        <%= atleta.status %>
                                    </span>
                                </td>
                                <td class="py-4 px-6 text-center">
                                    <form action="/delete/<%= atleta.id %>" method="POST"
                                        onsubmit="return confirm('⚠️ ¿Eliminar permanentemente a <%= atleta.name %>? Esto también borrará el cupón en Shopify.');">
                                        <button type="submit"
                                            class="text-gray-400 hover:text-red-500 transition p-2 rounded-full hover:bg-red-50"
                                            title="Eliminar">
                                            <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5" fill="none"
                                                viewBox="0 0 24 24" stroke="currentColor">
                                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                                                    d="M19 7l-.867 12.142A2 2 0 0116.138 21H7.862a2 2 0 01-1.995-1.858L5 7m5 4v6m4-6v6m1-10V4a1 1 0 00-1-1h-4a1 1 0 00-1 1v3M4 7h16" />
                                            </svg>
                                        </button>
                                    </form>
                                </td>
                            </tr>
                            <% }); %>
                                <% if (affiliates.length===0) { %>
                                    <tr>
                                        <td colspan="6" class="py-10 text-center text-gray-400 italic">
                                            No hay atletas registrados aún.
                                        </td>
                                    </tr>
                                    <% } %>
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</body>

</html>