<!DOCTYPE html>
<html lang="es">

<head>
    <meta charset="UTF-8">
    <title>Panel de Atletas</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body class="bg-orange-50 p-10 font-sans">
    <div class="max-w-4xl mx-auto bg-white p-8 rounded-lg shadow-xl border-t-4 border-orange-500">
        <h1 class="text-3xl font-bold mb-6 text-orange-800 flex items-center gap-2">
            🍊 Gestión de Cupones y Atletas
        </h1>

        <form action="/create-affiliate" method="POST"
            class="mb-10 bg-orange-50 p-6 rounded-lg border border-orange-200 shadow-sm">
            <h2 class="text-lg font-semibold mb-4 text-orange-900">Nuevo Atleta</h2>
            <div class="grid grid-cols-2 gap-4">
                <input type="text" name="name" placeholder="Nombre Atleta"
                    class="p-2 border rounded focus:border-orange-500 focus:outline-none" required>
                <input type="email" name="email" placeholder="Email"
                    class="p-2 border rounded focus:border-orange-500 focus:outline-none" required>
                <input type="text" name="code" placeholder="Código (ej: JUAN10)"
                    class="p-2 border rounded font-bold focus:border-orange-500 focus:outline-none" required>
                <div class="flex gap-2">
                    <input type="number" name="discount" placeholder="% Desc. Cliente"
                        class="p-2 border rounded w-1/2 focus:border-orange-500 focus:outline-none" required>
                    <input type="number" name="commission" placeholder="% Comisión"
                        class="p-2 border rounded w-1/2 focus:border-orange-500 focus:outline-none" required>
                </div>
            </div>
            <button type="submit"
                class="mt-4 bg-orange-600 text-white px-6 py-2 rounded hover:bg-orange-700 font-bold shadow transition hover:scale-105 transform">Crear
                Cupón en Shopify</button>
        </form>

        <div class="flex justify-between items-center mb-4">
            <h2 class="text-xl font-semibold text-gray-700">Lista de Afiliados</h2>
            <div class="flex gap-2">
                <a href="/sync"
                    class="bg-indigo-600 text-white px-4 py-2 rounded hover:bg-indigo-700 font-bold transition shadow-sm flex items-center gap-1">
                    <span>🔄</span> Sincronizar
                </a>
                <a href="/logout"
                    class="bg-gray-500 text-white px-4 py-2 rounded hover:bg-gray-600 font-bold transition shadow-sm">Cerrar
                    Sesión</a>
            </div>
        </div>
        <table class="w-full text-left border-collapse">
            <thead>
                <tr class="border-b-2 border-orange-200 bg-orange-50">
                    <th class="py-2 pl-2 text-orange-900">Atleta</th>
                    <th class="py-2 text-orange-900">Código</th>
                    <th class="py-2 text-center text-orange-900">Desc. Cliente</th>
                    <th class="py-2 text-center text-orange-900">Comisión</th>
                    <th class="py-2 text-center text-orange-900">Estado</th>
                    <th class="py-2 text-center text-orange-900">Acciones</th>
                </tr>
            </thead>
            <tbody>
                <% affiliates.forEach(function(atleta) { %>
                    <tr class="border-b hover:bg-orange-50 transition">
                        <td class="py-3 pl-2">
                            <%= atleta.name %>
                        </td>
                        <td class="py-3 font-mono font-bold text-orange-600">
                            <%= atleta.shopify_code %>
                        </td>
                        <td class="py-3 text-center">
                            <%= atleta.discount_percent %>%
                        </td>
                        <td class="py-3 text-center text-green-600 font-bold">
                            <%= atleta.commission_percent %>%
                        </td>
                        <td class="py-3 text-center">
                            <span
                                class="px-2 py-1 rounded text-xs <%= atleta.status == 'active' ? 'bg-green-100 text-green-800' : 'bg-red-100 text-red-800' %>">
                                <%= atleta.status %>
                            </span>
                        </td>
                        <td class="py-3 text-center">
                            <form action="/delete/<%= atleta.id %>" method="POST"
                                onsubmit="return confirm('¿Estás seguro de eliminar este cupón?');">
                                <button type="submit"
                                    class="bg-red-500 text-white px-3 py-1 rounded hover:bg-red-600 text-sm font-bold shadow-sm">Eliminar</button>
                            </form>
                        </td>
                    </tr>
                    <% }); %>
            </tbody>
        </table>
    </div>
</body>

</html>